mutation Login($input: LoginInput!) {
  login(input: $input) {
    accessToken
  }
}

mutation Register($input: LoginInput!) {
  register(input: $input) {
    id
    email
  }
}

mutation Logout {
  logout
}
